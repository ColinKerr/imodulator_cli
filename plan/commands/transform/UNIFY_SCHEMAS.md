# unify-schemas Command details

Implementation details for the `imod transform unify-schemas` command.

This command should generate a mapping file and use the transformation mechanism created for the `imod transform using-map` command.